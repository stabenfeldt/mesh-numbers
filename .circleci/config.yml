version: 2
jobs:
  build:
    working_directory: /numbers
    docker:
      - image: ruby:2.3-slim
    steps:
      - run: gem install bundler
      - checkout
      - run:
          command: |
            echo Running test
            rspec
